import styled from 'styled-components';

export const FollowUnfollowBtn = styled.button`
  background-color: var(--mainBlue);
  color: var(--white);
  border: none;
  padding: 0.18rem 0.5rem;
  border-radius: 4px;
  font-size: 1rem;
  cursor: pointer;
`;
