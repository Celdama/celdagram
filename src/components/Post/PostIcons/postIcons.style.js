import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  gap: 3px;

  .icon {
    height: 1.7rem;
    cursor: pointer;
  }

  .like {
    color: var(--redColor);
  }
`;
